// import alt from '../app/libs/alt'
// import ListActions from '../app/actions/ListActions'
// import ListStore from '../app/stores/ListStore'

// describe('ListStore', () => {
  // it('updates a list', () => {
    // let formerState = ListStore.getState().lists[0].title

    // let data = { title: "my new title" },
      // action = ListActions.update

    // alt.dispatcher.dispatch({action, data})
    // assert.equal(ListStore.getState().lists[0].title, data.title )
  // })
// })
